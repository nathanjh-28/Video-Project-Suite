import React, { useState, useEffect } from 'react';

/* 
mock data
    mock milestones
    mock projects
*/

const milestonesList = () => {

    // get projects with given milestone

    // use state

    // helper functions

    return (
        // Droppable area

        // map projects with draggable element
        <div>
            Milestone List Component
        </div>
    )
}

export default milestonesList;